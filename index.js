const employeesJSON = require('./employees.json');
const managersJSON = require('./managers.json');

// Code Here
class employees {
    constructor() 
   information = [];
       name; 
       positon; 
       yearJoined;
       salary;
      };
     return  idBadge (employees.positon, employees.name);

    class managers extends employees {
        constructor( ) {
            information = [];
            name; 
            positon; 
            yearJoined;
            salary;
            bonusPercentage;
            super(employees);
            this.magers = `bonusPercentage`;
    }
    
}
console.log ((this.mager = "bonusPercentage"));
    const salaryIncrease = ++ increaseAmount;

    const employees = [employees];
    information.map(employees);
    
    const mager = [this.mager];
    information.map(mager);

   // logManagerInfo () {
        name ; 
        since;
        bouns;

   // }

   // getWorkingYears (getWorkingYears) {
        const totalNumberOfYear = 2023- year;
        return (totalNumberOfYear);

  //  }

   const employee1 = "`employeesJSON`";
   if (this.information.name === "employeesJSON") {
information.map (`employeesJSON`);
   }

   const superHeroes = "";
   